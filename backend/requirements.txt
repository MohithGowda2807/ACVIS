# ─── ACVIS Backend Requirements ───────────────────────────────────────────
# Core API
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
pydantic>=2.6.0

# Database
pymongo[srv]>=4.6.0
dnspython>=2.6.0

# Auth
bcrypt>=4.1.0
pyjwt>=2.8.0

# AI / NLP — primary
groq>=1.1.0

# ML model (local fallback)
scikit-learn>=1.4.0
joblib>=1.3.0
pandas>=2.2.0
numpy>=1.26.0

# Sentence transformers (aspect normalization)
sentence-transformers>=2.7.0
torch>=2.2.0

# Language detection & translation
langdetect>=1.0.9
deep-translator>=1.11.4

# Caching (optional — pipeline continues if Redis unavailable)
redis>=5.0.0

# Utilities
openai>=1.0.0